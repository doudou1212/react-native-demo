import React, {Component} from 'react';
import {StyleSheet} from 'react-native'

const SearchResultsComponentStyle = StyleSheet.create({
  resultContainer: {
    marginTop: 10
  }
});

export default SearchResultsComponentStyle;